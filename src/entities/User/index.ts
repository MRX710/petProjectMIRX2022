export {
    getUserAuthData,
} from './model/selectors/getUserAuthData';

export {
    userActions,
    userReducer,
} from './model/slice/userSlice';

export type {
    IUser,
    IUserScheme,
} from './model/types/user';
